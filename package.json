{
  "name": "online-pathshala",
  "private": true,
  "scripts": {
    "start": "npm run start --prefix backend",
    "dev": "concurrently \"npm run start --prefix frontend\" \"npm run start --prefix backend\"",
    "client": "npm run start --prefix frontend",
    "server": "npm run start --prefix backend",
    "c": "cd frontend && npm install",
    "b": "cd backend && npm install",
    "nodePackage": "concurrently  \"npm run c\" \"npm run b\"",
    "install:backend": "npm install --prefix backend --omit=dev",
    "install:frontend": "npm install --prefix frontend --include=dev",
    "build": "npm run install:backend && npm run install:frontend && npm run build --prefix frontend"
  },
  "dependencies": {
    "concurrently": "9.2.1"
  },
  "overrides": {
    "shell-quote": "1.8.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}